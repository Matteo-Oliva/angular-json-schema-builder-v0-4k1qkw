import { FormGroup, FormBuilder } from '@angular/forms';

export class ObjectGroup {
  formGroup: FormGroup;

  // constructor(private fb: FormBuilder, properties, required?) {
  //   console.log(properties);
  //   this.buildForm(properties, required)
  // }

  // private buildForm(properties, required?) {
  //   this.formGroup = this.fb.group({

  //   })
  // }
}
